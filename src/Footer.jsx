const Footer = () => {
    return (
        <footer className="bg-primary text-white text-center py-4 fixed bottom-0 w-full">
            <p>Copyright &copy; {new Date().getFullYear()} Alphabeta</p>
        </footer>
    );
}

export default Footer;